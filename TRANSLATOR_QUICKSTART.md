# 🚀 Быстрый старт AI Переводчика

## 1️⃣ Получите API ключ (2 минуты)

1. Перейдите на [OpenRouter](https://openrouter.ai/keys)
2. Зарегистрируйтесь / войдите через Google
3. Создайте новый API ключ
4. Скопируйте его (начинается с `sk-or-v1-...`)

## 2️⃣ Настройте .env (1 минута)

Создайте файл `.env.local` в корне проекта:

```bash
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь
```

## 3️⃣ Запустите проект (30 секунд)

```bash
npm run dev
```

## 4️⃣ Откройте переводчик

```
http://localhost:3000/translator
```

---

## ✅ Готово! Можно переводить

### Пример использования:

1. **Выберите языки** — например, English → Russian
2. **Вставьте текст** — любой размер (даже 100k+ символов)
3. **Нажмите "Перевести"** — подождите несколько секунд
4. **Скопируйте результат** — готово!

---

## 💡 Советы

- Переводчик работает с текстами **любого размера**
- Автоматически разбивает на чанки по **3500 символов**
- Переводит **параллельно** до 5 чанков одновременно
- Стоимость: **~$0.003-0.007** за 100k символов

---

## 🐛 Если не работает

### Ошибка "API ключ не настроен"
✅ Проверьте файл `.env.local` — он должен быть в **корне проекта**

### Ошибка 401 Unauthorized
✅ Проверьте правильность API ключа на [OpenRouter](https://openrouter.ai/keys)

### Медленно переводит
✅ Это нормально для больших текстов. 100k символов ~ 15-25 секунд.

---

## 📊 Примеры времени обработки

| Размер | Время |
|--------|-------|
| 1000 символов | ~1-2 сек |
| 10000 символов | ~2-4 сек |
| 50000 символов | ~8-15 сек |
| 100000 символов | ~15-25 сек |

---

**Приятного использования! 🎉**
