# 🔍 Keywords Collector - Инструкция по интеграции

## 📋 Что это?

6-й инструмент для SEO-платформы: сбор ключевых слов через API Keys.so с фильтрацией и экспортом в CSV.

---

## 🚀 Шаги для интеграции в проект

### 1️⃣ Скопируйте файлы в ваш проект

```bash
cd ~/Desktop/redirect-checker

# Скопируйте новую страницу
cp /path/to/keywords-collector-files/app/keywords-collector/page.tsx app/keywords-collector/page.tsx

# Скопируйте API роут
cp /path/to/keywords-collector-files/app/api/keywords-collector/route.ts app/api/keywords-collector/route.ts

# Обновите навигацию
cp /path/to/keywords-collector-files/components/Navigation.tsx components/Navigation.tsx
```

### 2️⃣ Проверьте локально

```bash
npm run dev
```

Откройте: `http://localhost:3000/keywords-collector`

### 3️⃣ Задеплойте на Vercel

```bash
git add .
git commit -m "Add Keywords Collector - 6th SEO tool with Keys.so API integration"
git push origin main
```

Vercel автоматически задеплоит изменения.

---

## ✨ Функционал

### Основные возможности:
- ✅ Ввод API ключа Keys.so
- ✅ Загрузка списка URL (textarea или файл .txt)
- ✅ Фильтры:
  - Максимальная позиция (по умолчанию ≤10)
  - Минимальная !Частотность (по умолчанию >1)
- ✅ Real-time прогресс для каждого URL
- ✅ Таблица результатов с 7 колонками
- ✅ Экспорт в CSV с кодировкой UTF-8

### Технические особенности:
- 🔄 Server-Sent Events (SSE) для потоковой передачи данных
- ⏱️ Задержки между запросами (1 сек) для защиты от rate limiting
- 🎨 Responsive дизайн в стиле остальных инструментов
- 🛡️ Обработка ошибок API Keys.so

---

## 📊 Структура данных

### Входные параметры:
```typescript
{
  apiKey: string;           // API ключ Keys.so
  urls: string[];           // Массив URL
  minFrequency: number;     // Минимальная !Частотность (default: 1)
  maxPosition: number;      // Максимальная позиция (default: 10)
}
```

### Выходные данные:
```typescript
{
  url: string;              // URL страницы
  keyword: string;          // Ключевое слово
  position: number;         // Позиция в выдаче
  frequency: number;        // Частотность (ws)
  exactFrequency: number;   // !Частотность (wsk)
  competition: string;      // Конкурентность
  date: string;             // Дата сбора (YYYY-MM-DD)
}
```

---

## 🔧 API Keys.so

### Эндпоинт:
```
GET https://api.keys.so/report/simple/organic/keywords/bypage
```

### Параметры запроса:
- `domain` - домен сайта (example.com)
- `page_url` - путь страницы (/category/product/)
- `base` - регион (msk)
- `per_page` - количество результатов (100)
- `page` - номер страницы
- `filter` - фильтр позиций (pos<=10)

### Headers:
```
X-Keyso-TOKEN: your_api_key_here
Content-Type: application/json
Accept: application/json
```

---

## 🎯 Логика работы

1. **Парсинг URL**: Извлечение домена и пути страницы
2. **Пагинация**: Последовательный обход всех страниц результатов
3. **Фильтрация**: 
   - API фильтрует по позиции (pos<=maxPosition)
   - Сервер фильтрует по !Частотности (wsk>minFrequency)
4. **Streaming**: Real-time обновление прогресса через SSE
5. **Экспорт**: CSV с BOM для корректной кодировки в Excel

---

## ⚠️ Важные моменты

### Rate Limiting:
- Задержка 1 секунда между запросами страниц
- Задержка 5 секунд при статусе 202 (данные обрабатываются)

### Обработка ошибок:
- Если URL вернул ошибку, добавляется строка с текстом ошибки
- Обработка продолжается для остальных URL

### Производительность:
- Потоковая обработка (SSE) - результаты появляются по мере сбора
- Прогресс-бары для каждого URL отдельно
- Автоматическое обновление таблицы результатов

---

## 📁 Структура файлов

```
redirect-checker/
├── app/
│   ├── keywords-collector/
│   │   └── page.tsx              # UI компонент (17KB)
│   └── api/
│       └── keywords-collector/
│           └── route.ts          # API endpoint (7.5KB)
├── components/
│   └── Navigation.tsx            # Обновленная навигация (2.7KB)
└── README.md
```

---

## 🧪 Тестирование

### Тестовые данные:
```
https://www.vazaro.ru/catalogue-sets/termosy-1-litr/
https://www.vazaro.ru/catalogue-sets/termosy-350-ml/
https://www.vazaro.ru/catalogue-sets/yaponskie-farforovye-chajniki/
```

### Ожидаемый результат:
- Собранные ключевые слова с позицией ≤10
- Отфильтрованные по !Частотности >1
- Таблица с колонками: URL, Ключевое слово, Позиция, Частотность, !Частотность, Конкурентность, Дата

---

## 🎨 UI/UX особенности

- 🟣 **Градиентный фон**: purple-50 → pink-50
- 📊 **Цветовая индикация позиций**:
  - 1-3 позиция: зеленый
  - 4-10 позиция: желтый
  - >10 позиция: серый
- 📈 **Статусы обработки**:
  - Ожидание (серый)
  - Обработка (синий)
  - Завершено (зеленый)
  - Ошибка (красный)
- 🔄 **Real-time прогресс**: процент + счетчик найденных ключевых слов

---

## 🚀 Готово к деплою!

После копирования файлов проект готов к публикации на Vercel.

**Следующие шаги**:
1. Проверьте локально: `npm run dev`
2. Задеплойте: `git add . && git commit -m "Add Keywords Collector" && git push`
3. Vercel автоматически задеплоит изменения

---

## 📞 Поддержка

Если возникнут вопросы:
- Проверьте API ключ Keys.so
- Убедитесь, что URL корректны
- Проверьте лимиты API Keys.so

**Готово! 🎉**
